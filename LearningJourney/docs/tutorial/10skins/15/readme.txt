Extending a skin: overriding standard macros
----------------------------------------------

* added a browser:page skin_macros that overrides the default macros. Notice that
  skin_macros is based on the template_tablelayout.pt, so skinmacros actually tells the site
  what it will look like
